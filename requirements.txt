# Termux Compatible Requirements

# Core Dependencies
google-api-python-client==2.88.0
google-auth-oauthlib==1.0.0
google-auth-httplib2==0.1.0
requests==2.31.0
python-dotenv==1.0.0

# Web Framework (lightweight for Termux)
flask==2.3.2

# Video Processing (Termux compatible versions)
yt-dlp>=2024.1.0
Pillow>=9.0.0

# Telegram Bot
python-telegram-bot==20.3

# AI/ML (lightweight)
groq==0.4.2

# Utilities
schedule==1.2.0

# Note: MoviePy and OpenCV may have issues in Termux
# Use alternative lightweight video processing if needed
